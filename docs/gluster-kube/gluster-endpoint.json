{
    "kind": "Endpoints",
    "apiVersion": "v1",
    "metadata": {
        "name": "glusterfs-cluster"
    },
    "subsets": [{
        "addresses": [{
            "ip": "$LOCAL_IP"
        }],
        "ports": [{
            "port": 1,
            "protocol": "TCP"
        }]
    }]
}
