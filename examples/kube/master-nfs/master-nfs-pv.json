{
  "apiVersion": "v1",
  "kind": "PersistentVolume",
  "metadata": {
    "name": "master-nfs-pv"
  },
  "spec": {
    "capacity": {
        "storage": "150M"
    },
    "accessModes": [ "ReadWriteMany" ],
    "nfs": {
        "path": "$NFS_SHARE_PATH",
        "server": "$NFS_SHARE_IP"
    },
    "persistentVolumeReclaimPolicy": "Retain"
  }
}
